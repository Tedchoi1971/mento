document.addEventListener('click',e=>{const a=e.target.closest('[data-action]')?.dataset.action;if(a==='new-grant')modal(`<h2>신규 공고 등록</h2><p>공고 기본정보와 원문을 등록하면 AI 분석 대상으로 전환됩니다.</p><form id="grant-form"><div class="form"><div class="field full"><label>공고명 *</label><input name="title" required placeholder="공모사업 공식 명칭"></div><div class="field"><label>주관기관 *</label><input name="agency" required></div><div class="field"><label>접수 마감일</label><input name="deadline" type="date"></div><div class="field"><label>최대 지원금</label><input name="budget" placeholder="예: 2억원"></div><div class="field"><label>공고 URL</label><input name="url" type="url" placeholder="https://"></div><div class="field full"><label>공고문 파일</label><input type="file" accept=".pdf,.hwp,.hwpx,.doc,.docx"></div><div class="field full"><label>관리 메모</label><textarea name="memo" placeholder="담당자, 중요조건, 확인사항"></textarea></div></div><div class="actions"><button type="button" class="ghost" data-action="close">취소</button><button class="primary">등록 후 분석 대기</button></div></form>`);if(a==='grant-detail')modal(`<h2>공고 분석 정보</h2><p>공고 원문에서 구조화하여 관리할 항목입니다.</p>${['신청대상·기업규모·업종','소재지역·업력·재무요건','지원분야·기술분야·TRL','지원금·자부담·사업기간','필수 제출서류·제외조건','평가항목·배점·가점'].map((x,i)=>`<div class="insight"><b>관리항목 0${i+1}</b><p>${x}</p></div>`).join('')}`)});
document.addEventListener('submit',e=>{if(e.target.id==='grant-form'){e.preventDefault();close();toast('신규 공고를 분석 대기 상태로 등록했습니다.')}});
